# Setup Next Steps

## Immediate technical tasks
1. install Vite and TypeScript in the workspace
2. wire the website and playground package scripts to Vite commands
3. unify compiler exports so the public entry is the default integration path
4. connect the playground example registry to a selector UI
5. add a small build verification step

## Repository cleanup later
- remove stale placeholder paths
- migrate legacy root pages gradually
- keep the visual identity while replacing the weak internals
